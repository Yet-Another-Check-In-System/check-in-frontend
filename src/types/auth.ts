export type LoginUser = {
    email: string;
    password: string;
};

export type RegisterUser = {
    firstName: string;
    lastName: string;
    company: string;
    country: string;
    email: string;
    password: string;
    passwordAgain: string;
};
